# Environment variables (NEVER commit)
.env
.env.local
.env.*.local

# Node modules
/node_modules

# Compilation output
/dist

# pnpm deploy output
/bundle

# Hardhat Build Artifacts
/artifacts

# Hardhat compilation (v2) support directory
/cache

# Typechain output
/types

# Hardhat coverage reports
/coverage

deploy-zksync/.env

# Datasets de imágenes y archivos zip (muy pesados — no subir a GitHub)
dataset/
dataset_split/
dataset_balanced/
dataset_balanced_split/
dataset_raw/
dataset.zip
dataset (2).zip

# Modelos base y entrenados (archivos *.pt y carpeta runs)
*.pt
yolov8*.pt
runs/

# Archivos de caché de Python
__pycache__/
*.pyc